#!/bin/bash

SSID=$1

if [[ -z $SSID ]]; then
    echo Usage: $1 SSID
    exit 1
fi

trap exit TERM INT

while :; do
    wait-beacon "$SSID" || continue
    join-adhoc "$SSID" || continue
    wait-disconnect
done
